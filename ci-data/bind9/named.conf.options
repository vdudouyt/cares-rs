options {
	directory "/var/cache/bind";
	listen-on-v6 { any; };
	recursion no;         // do not query upstream DNS
	allow-query { any; }; // allow local queries
	forwarders {};        // no forwarders
};
